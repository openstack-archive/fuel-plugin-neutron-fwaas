attributes:
  # Show fwaas only for neutron
  metadata:
    restrictions:
      - condition: cluster:net_provider != 'neutron'
        action: hide
      - condition: settings:neutron_advanced_configuration.neutron_dvr.value == true
        message: "DVR must be disabled in order to use FWaaS plugin"
